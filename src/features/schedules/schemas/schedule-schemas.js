import{z}from'zod';export const scheduleSchema=z.object({id:z.string().uuid(),createdAt:z.string(),doctorId:z.string().uuid(),doctorName:z.string(),availableDate:z.string(),startTime:z.string(),endTime:z.string(),booked:z.boolean(),isActive:z.boolean()});export const scheduleRequestSchema=z.object({doctorId:z.string().uuid('Selecciona un médico'),availableDate:z.string().min(1,'Indica la fecha'),startTime:z.string().min(1,'Indica la hora inicial'),endTime:z.string().min(1,'Indica la hora final')}).refine(v=>v.endTime>v.startTime,{path:['endTime'],message:'La hora final debe ser posterior'});export const generateScheduleSchema=z.object({doctorId:z.string().uuid('Selecciona un médico'),fromDate:z.string().min(1,'Indica la fecha inicial'),toDate:z.string().min(1,'Indica la fecha final')}).refine(v=>v.toDate>=v.fromDate,{path:['toDate'],message:'La fecha final no puede ser anterior'})
