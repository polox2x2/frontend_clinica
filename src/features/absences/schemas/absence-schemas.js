import{z}from'zod';export const absenceSchema=z.object({id:z.string().uuid(),createdAt:z.string(),doctorId:z.string().uuid(),doctorName:z.string(),startDate:z.string(),endDate:z.string(),reason:z.string().nullish(),isActive:z.boolean()});export const absenceRequestSchema=z.object({doctorId:z.string().uuid('Selecciona un médico'),startDate:z.string().min(1,'Indica la fecha inicial'),endDate:z.string().min(1,'Indica la fecha final'),reason:z.string().trim().transform(v=>v||null)}).refine(v=>v.endDate>=v.startDate,{path:['endDate'],message:'La fecha final no puede ser anterior'})
